#include<bits/stdc++.h> 
using namespace std;

int main()
{
    long long u,v,x,y;
    int t;
    cin>>t;
    while(t--)
    {
        cin>>u>>v;
        x = - (u*u);
        y = (v*v);
        cout<<x<<" "<<<<endl;
    }
    return 0;
}